name: remote release

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'tag'
        required: true
jobs:
  call:
    uses: mattkasun/test2/.github/workflows/sub/junk.yml@master
    with:
      version: ${{ inputs.version }}
    secrets: inherit
    
  
