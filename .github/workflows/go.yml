name: Go

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
  workflow_dispatch:

jobs:

  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2

 #   - name: Set up Go
 #     uses: actions/setup-go@v2
 #     with:
 #      go-version: 1.17

 #   - name: Build
 #     run: go build -v ./...

    - name: send message
      uses: appleboy/discord-action@master
      with:
        webhook_id: ${{ secrets.GITHUB_WEBHOOK_ID }}
        webhook_token: ${{ secrets.GITHUB_WEBHOOK_TOKEN }}
        color: "#48f442"
        username: "GitHub Bot"
        message: "A new commit has been pushed with custom color."
